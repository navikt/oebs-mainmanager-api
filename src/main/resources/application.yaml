
server:
  port: 8080

no.nav.security.jwt:
   issuer:
     azure:
       accepted_audience: ${AZURE_APP_CLIENT_ID}
       discovery-url: ${AZURE_APP_WELL_KNOWN_URL}

# Add these if needed for SSL

spring:
  datasource:
    hikari:
      data-source-properties:
        oracle:
          net:
            ssl_version: 1.2
            ssl_server_dn_match: true
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

    driver-class-name: oracle.jdbc.OracleDriver
    username: ${OEBS_DB_USERNAME}
    password: ${OEBS_DB_PASSWORD}
    url: ${ORACLE_URL}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        temp:
        use__jdbc_metadata_defaults: false
        dialect: org.hibernate.dialect.OracleDialect
    show-sql: false
    open-in-view: false
  application:
    name: oebs-mainmanager-api

mainmanager:
  user: ${MM_USER}
  password: ${MM_PASSWORD}
  grant_type: ${MM_GRANT_TYPE}
  url_token: ${MM_URL_TOKEN}
  artikkelinfo: ${MM_ARTIKKELINFO}
  art: ${MM_ART}
  felles: ${MM_FELLES}
  kostnadssted: ${MM_KOSTNADSSTED}
  product: ${MM_PRODUKT}
  statsregnskapskonto: ${MM_STATSREGNSKAPSKONTO}
  vendors: ${MM_VENDORS}
  oppgave: ${MM_OPPGAVE}

swagger:
  oebs_env: oebs
  app_update: 22.09.26
  app_version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics

springdoc:
  packagesToScan: no.nav.oebs.api.controller
  override-with-generic-response: false
  remove-broken-reference-definitions: false
  swagger-ui:
    disable-swagger-default-url: true
    tags-sorter: alpha

logging:
  level:
    oracle:
      jdbc: WARN
      net: WARN
    org:
       hibernate: ERROR
